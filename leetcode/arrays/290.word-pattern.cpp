/**
 *  I have  a hashmap => key = element from the pattern
 *                       map = corres element from the target string
 * 
 *  # Start traversing the words inside the target string
 *    ans build my hashmap at the same time
 *      a : dog
 *      b : cat
 * 
 * */
class Solution {
public:
    bool wordPattern(string pattern, string s) {
        
    }
};
// @lc code=end

